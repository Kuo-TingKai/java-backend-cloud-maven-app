# Server Configuration
server.port=8080
server.servlet.context-path=/

# Application Configuration
spring.application.name=cloud-strategy

# Logging Configuration
logging.level.org.example=INFO
logging.level.org.springframework.web=INFO

# Database Configuration (Optional, enable as needed)
# spring.datasource.url=jdbc:mysql://localhost:3306/cloud_strategy
# spring.datasource.username=root
# spring.datasource.password=password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# MongoDB Configuration (Optional)
# spring.data.mongodb.uri=mongodb://localhost:27017/cloud_strategy

# Redis Configuration (Disabled for demo)
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.data.redis.password=
# spring.data.redis.database=0

# RabbitMQ Configuration (Optional)
# spring.rabbitmq.host=localhost
# spring.rabbitmq.port=5672
# spring.rabbitmq.username=guest
# spring.rabbitmq.password=guest

# Kafka Configuration (Optional)
# spring.kafka.bootstrap-servers=localhost:9092
# spring.kafka.consumer.group-id=cloud-strategy-group
# spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
# spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
# spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
# spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# AWS Configuration (Optional)
# aws.accessKeyId=your-access-key
# aws.secretKey=your-secret-key
# aws.region=us-east-1
# aws.s3.bucket=your-bucket-name

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

# JVM Optimization
# -Xms512m -Xmx2g -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# Exclude database auto-configuration, required when no database is available
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.redisson.spring.starter.RedissonAutoConfiguration